import { Normalize } from 'styled-normalize';
import { GlobalStyle } from './GlobalStyle';

const App = () => (
  <>
    <GlobalStyle />
    <Normalize />
    <div>Hello world!</div>
  </>
);

export default App;
